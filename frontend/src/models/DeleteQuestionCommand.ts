/**
 * Command to delete a question logically.
 *
 * The command carries no data because the question identifier is part of the
 * route.
 */
// eslint-disable-next-line @typescript-eslint/no-empty-object-type
export interface DeleteQuestionCommand {}
