/**
 * Command to duplicate an existing question.
 *
 * The source question identifier is part of the route; the command carries no
 * additional data.
 */
// eslint-disable-next-line @typescript-eslint/no-empty-object-type
export interface DuplicateQuestionCommand {}
